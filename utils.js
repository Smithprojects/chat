export function scrollBlockInBottom() {
    let block = document.querySelector(".messages__wrap");
    block.scrollTop = block.scrollHeight;
    // console.log('scroll')

    // element.scrollTop = element.scrollHeight;
}